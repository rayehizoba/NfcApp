import {types} from './app.reducer';

/**
 *
 * @param data
 * @returns {function(*): *}
 */
// export const trackScreenView = data => dispatch =>
//   dispatch({type: types.TRACK_SCREEN_VIEW, data});
